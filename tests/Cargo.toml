[package]
name = "tests"
description = "tests for `derive_aliases`"
publish = false
version = "0.1.0"
edition = "2024"

[dependencies]
derive_aliases = { path = "../crate" }

[features]
default = ["serde", "arbitrary", "smallvec"]

serde = []
arbitrary = []
smallvec = []
